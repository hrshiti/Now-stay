const fs = require('fs'); const lines = fs.readFileSync('src/app/partner/pages/AddTentWizard.jsx', 'utf8').split('\n'); let depth = 0; for(let i=931; i<1775; i++) { const line = lines[i]; if (!line) continue; depth += (line.match(/<div[\s>]/g) || []).length; depth -= (line.match(/<\/div>/g) || []).length; if (depth <= 0) console.log('Zero depth at line ' + (i+1)); } console.log('Final depth: ' + depth);
